<section class="devise">
  <h1>Edit Your BMR</h1>

  <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
    <%= devise_error_messages! %>
  
    <div>BMR<br />
    <%= f.text_field :bmr, autofocus: true %></div>

    <div><%= f.label :current_password %><br />
    <%= f.password_field :current_password, placeholder: "Your current password is needed to confirm the change" %></div>

    <div><%= f.submit "Update" %></div>
  <% end %>

</section>